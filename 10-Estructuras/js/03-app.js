//Comparacion con < y >, tambien podemos usar mayor o iguual o menor o igual.

const  dinero = 500,
      totalPagar = 500

if ( dinero >= totalPagar ) {
  console.log('Puedes pagar');
} else {
  console.log('Fondos insuficientes.');
}